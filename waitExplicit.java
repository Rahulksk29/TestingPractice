public class waitExplicit {

/*
    public static void waitForElementVisible(WebElemnt element, long timeout){

        try{
        WebDriver wait = new webDriver wait(timeout);
        webDriver.until(ExpectedConditions.visiblityOf(element));
        }catch (ElementNotVisisbleException e){
        System.out.println("Element not present: " + e);
        System.out.println(e.getText());
        }


    }
*/

}
